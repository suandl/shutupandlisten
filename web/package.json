{
  "name": "shutupandlisten-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Rung 1 in-browser quiet-companion harness — U3 timing (VAD + silence floor + smart-turn asymmetric veto) + U4 on-device STT (Moonshine/Whisper, CPU·WASM) + U5 response-hierarchy gate + on-device listener LLM (WebGPU) + U6 on-device TTS (CPU·WASM) closing the end-to-end warmed loop with per-stage instrumentation.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test 'src/**/*.test.ts' 'e2e/**/*.test.ts' 'scripts/**/*.test.mjs'",
    "test:watch": "node --test --watch 'src/**/*.test.ts' 'e2e/**/*.test.ts' 'scripts/**/*.test.mjs'",
    "provision:stt": "node scripts/provision-stt.mjs",
    "provision:llm": "node scripts/provision-llm.mjs",
    "provision:tts": "node scripts/provision-tts.mjs",
    "provision:denoise": "node scripts/provision-denoise.mjs",
    "provision:smart-turn": "node scripts/provision-smart-turn.mjs",
    "measure": "node src/measure.ts",
    "measure:race": "node src/measure-race.ts",
    "measure:loop": "node scripts/loop-latency.mjs",
    "demo:capture": "node e2e/capture.ts",
    "demo:u6": "node e2e/capture.ts e2e/demos/u6-warmed-loop.md",
    "works-check": "node scripts/works-check.mjs"
  },
  "dependencies": {
    "@ricky0123/vad-web": "^0.0.24",
    "onnxruntime-web": "^1.20.1"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}

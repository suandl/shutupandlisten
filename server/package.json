{
  "name": "shutupandlisten-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Customer-facing proxy for shutupandlisten: holds the Anthropic key, meters per-user substantive-tier listener turns and coverage checks.",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test 'src/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.113.0",
    "jose": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
